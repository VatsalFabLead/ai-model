fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
numpy>=1.26.0
slowapi>=0.1.9
httpx>=0.27.0
a2wsgi>=1.10.0

# --- Optional: free open-source LLM backend (MODEL_BACKEND=llm) ---
# llama-cpp-python needs a PREBUILT CPU wheel (no compiler required).
# Use Python 3.10-3.12 (NOT 3.14) and install with:
#   pip install "llama-cpp-python==0.2.90" \
#     --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu \
#     --only-binary=:all:
# Then download a model:  python scripts/download_model.py
